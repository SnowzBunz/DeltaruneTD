# DeltaruneTD

Repository for DeltaruneTD project.

Quick notes
- This repo contains JSON config/data files used by the project.
- Use Git from the command line or VS Code Source Control to make commits and push to GitHub.

How I pushed the current changes
1. I added a `.gitignore` and `.gitattributes` to the repo root.
2. I committed the modified `TimeUnitGiveout.json` along with those files.
3. I pushed to `origin/main`.

If you want different files included in the initial commit or a specific commit message, let me know.
[SPAMTON. G SPAMTON. WAS HERE KID!!]


